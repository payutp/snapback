<% if current_user %>
<div>
  <%= button_to 'New Lend', new_lend_path, :method=>:get, :class=>"btn btn-primary float" %>
  <%= button_to 'New Return', new_return_path, :class=>"btn btn-primary float" %>
</div>
<br><br>
  <h1>My Active Lend Requests</h1>

  <table class="table table-striped">
    <tr>
      <th>Status</th>
      <th>To</th>
      <th> Item </th>
      <th></th>
      <th></th>
    </tr>

  <% @lends_open.each do |lend| %>
    <tr>
      <td><%= lend.status %></td>
      <td>
        <% if lend.to_id == nil %>
          <%= "Waiting for a nice person to lend" %>
        <% else %>
          <%= User.find(lend.to_id).email %>
        <% end %>
      </td>
      <td><%= lend.item.name %> </td>
      <td><%=button_to "Cancel", lend, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-small btn-danger"%></td>
    </tr>
  <% end %>

  <% @lends_pending.each do |lend| %>
    <tr>
      <td><%= lend.status %></td>
      <td>
        <% if lend.to_id == nil %>
          <%= "Waiting for a nice person to lend" %>
        <% else %>
          <%= User.find(lend.to_id).email %>
        <% end %>
      </td>
      <td><%= lend.item.name %> </td>
<<<<<<< HEAD
      <td><%=button_to "Return", root_url, method: :get, :class => "btn btn-small btn-success" %> </td>
      <td><%= button_to "Cancel", lend, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-small btn-danger" %> </td>
=======
      <td><%=button_to "Return", return_item_path(lend.item.id), method: :post, :class => "btn btn-small btn-success" %> </td>
>>>>>>> 0ed2987d79dc249fcb27d25da69d9bff5ce9d0c9
    </tr>
  <% end %>
  </table>

  <br />

  <h1> People are looking for these items: </h1>

  <table class="table table-striped">
  <tr>
    <th> User </th>
    <th> Item </th>
    <th> </th>
  </tr>

  <% @lends_others.each do |lend| %>
  <tr>
  <td> <%= User.find(lend.user_id).email %></td>
  <td> <%= lend.item.name %></td>
  <td><%=button_to "Accept", new_return_path(:id => lend.id), :class => "btn btn-small btn-success"%></td>
  </tr>
  <% end %>
  </table>
<% else %>
<div class="hero-unit"><h1>About</h1> <br>With SnapBack, you can lend and borrow, with ease with the knowledge that you'll find what you need and get what you lend back in a snap! 
  <div >
  <%= button_to "Sign up", signup_path, :method => "get", :class => "btn btn-large btn-primary float" %>
  <%= button_to "Log in", login_path, :method => "get", :class => "btn btn-large btn-primary float" %>
  </div>
</div>
  <br><br><br>
<% end %>